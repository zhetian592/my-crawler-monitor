name: Tier1 - Auto (每小时 9-19点)

on:
  schedule:
    # UTC 1-11点 → 北京时间 9-19点，每小时一次
    - cron: '0 1-11 * * *'
  workflow_dispatch:   # 保留手动触发，供你自己使用

jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests feedparser beautifulsoup4 openai

      - name: Run crawler
        env:
          GH_MODELS_TOKEN: ${{ secrets.GH_MODELS_TOKEN }}
        run: python crawler.py

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto update $(date -u '+%Y-%m-%d %H:%M:%S')"
          file_pattern: "data/* report.md report.html reports/*"
          commit_user_name: "github-actions[bot]"
          commit_user_email: "github-actions[bot]@users.noreply.github.com"
          commit_author: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
